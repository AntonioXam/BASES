DELIMITER $$

DROP FUNCTION IF EXISTS es_par $$

CREATE FUNCTION es_par(a INT) RETURNS INT
BEGIN 
	DECLARE par INT;
	IF MOD(a,2)=0 THEN
		SET par=TRUE;
	ELSE
		SET par=FALSE;
	END IF;
	RETURN(par);
END $$

DELIMITER ;